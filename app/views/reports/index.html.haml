%h1 Reports

- if @reports.any?
  %table{class: "table table-striped"}
    %thead
      %tr
        %th Url
        %th Status
        %th
    %tbody
      - @reports.each do |report|
        %tr{:class => (report.is_valid ? 'valid' : 'invalid' )}
          %td= report.url
          %td= report.status_code
          %td= link_to 'Detail', project_report_path(@project, report)