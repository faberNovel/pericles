.page-header
  %h1
    !="#{@resource.name} <small> Edit Representation: #{@representation.name} </small>"

= render 'form'

= link_to 'Back', resource_resource_representation_path(@resource, @representation)