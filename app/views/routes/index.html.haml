
%h1 Routes

- if @routes_by_resource.any?
  - @resources.each do |resource|
    .routes-container
      %a{href: project_resource_path(@project, resource)}
        %h4= resource.name
      = link_to 'New Route', new_resource_route_path(resource), class: "btn btn-default btn-lg"

    .routes-sub-container
      - @routes_by_resource[resource].each do |route|
        %a{class: 'route-row', href: resource_route_path(route.resource, route)}
          %div{style: 'width: 10%'}
            %span{ class: label_class_for_http_method(route.http_method) }= route.http_method
          %div{style: 'width: 35%'}= route.url
          %div{style: 'width: 35%'}= route.resource_representations.uniq.map(&:name).join(", ")
- else
  %p
    %i= t('.no_route_placeholder')
