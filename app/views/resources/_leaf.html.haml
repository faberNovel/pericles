%a{class: "list-group-item", href: project_resource_path(project, resource)}
  .flexcontainer.flexcontainer-space-between
    .name
      = '      ' * indentation + (indentation.zero? ? '' : '↳ ') + resource.name
    - if resource.has_invalid_mocks?
      .error
        .tool-tip{'data-toggle' => "tooltip", 'data-placement' => "top", title: "Invalid mocks !"}
          \/!\
- (resource.used_resources - rendered_resources).each do |sub_resource|
  = render partial: 'leaf', locals: {resource: sub_resource, indentation: indentation + 1, rendered_resources: rendered_resources + [sub_resource]}

