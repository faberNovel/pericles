package <%= android_company_domain_name =%>.<%= project.title.parameterize(separator: '.') =%>.android.data.net.retrofit.model

data class <%= resource.rest_name =%>(
<% resource.resource_attributes.order(:name).decorate.each do |attribute| %>
    val <%= attribute.variable_name =%>: <%= attribute.kotlin_type =%><%= (attribute ==  resource.resource_attributes.order(:name).last) ? '' : ',' =%>

<% end %>
)
