//
//  <%= resource.rest_name =%>.swift
//
//  Generated by Pericles on <%= Date.today.strftime("%d/%m/%Y") =%>

//
//

import Foundation

struct <%= resource.rest_name =%> : Decodable {
<% resource.resource_attributes.each do |attribute| %>
    let <%= attribute.camel_variable_name =%>: <%= attribute.swift_type =%>

<% end %>

    enum CodingKeys : String, CodingKey {
    <% resource.resource_attributes.each do |attribute| %>
        case <%= attribute.camel_variable_name =%><% if attribute.camel_variable_name != attribute.key_name %> = "<%= attribute.key_name =%>"<% end %>
    <% end %>
    }
}
