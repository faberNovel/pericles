- content_for :page_title do
  %h1 Projects

.row
  .col-xs-12
    .list-group
      - @projects.each do |project|
        %a{class: "list-group-item", href: project_path(project)}
          = project.title