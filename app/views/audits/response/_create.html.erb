<div>
  A new <%= audit.type %> <%= audit.name %> has been created:
  <% if audit.url %>
    <%= link_to audit.link_name, audit.url %>
  <% else %>
    Audit which no longer exists
  <% end %>
</div>
