<%= audit.auditable_type %> <% if audit.url %><<%= audit.url %>|<%= audit.link_name %>><% else %>Audit which no longer exists<% end %> has been updated:
<%= render 'audits/changes', audited_changes: audit.map_changes %>