%h3 Metadatum

= render metadatum, show: false

%h3 Mocks
.flexcontainer-justify-end
  = link_to 'New mock', new_metadatum_metadatum_instance_path(metadatum), class: "btn btn-primary" if user.can_create? MetadatumInstance, project: project
.margin-top-element
  - metadatum.metadatum_instances.each do |instance|
    = render instance