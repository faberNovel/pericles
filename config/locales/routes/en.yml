en:
  activerecord:
    errors:
      models:
        route:
          attributes:
            request_body_schema:
              schema_must_be_valid_json: "The input JSON Schema is not a valid JSON text (RFC 7159)"
              schema_must_respect_json_schema_spec: "The input JSON Schema does not conform to JSON Schema Draft 4. Error: %{error}"
            body_schema:
              schema_must_be_valid_json: "The input JSON Schema (body) is not a valid JSON text (RFC 7159)"
              schema_must_respect_json_schema_spec: "The input JSON Schema (body) does not conform to JSON Schema Draft 4. Error: %{error}"
            response_schema:
              schema_must_be_valid_json: "The input JSON Schema (response) is not a valid JSON text (RFC 7159)"
              schema_must_respect_json_schema_spec: "The input JSON Schema (response) does not conform to JSON Schema Draft 4. Error: %{error}"