en:
  activerecord:
    errors:
      models:
        resource_representation:
          attributes:
            base:
              destroy_failed_foreign_key: "Could not destroy resource_representation because it is still referenced in responses."